# Activator code review — 2026-09-15

## 来源与整理

来源：用户提供的 `05_scripts(1).zip`。其中 1 个 `.mjs`、2 个 `.py` 是源码，2 个 `.pyc` 是可再生的 Python 3.12 缓存，未纳入源码仓库。来源哈希与文件映射见 `SOURCE_ARCHIVE.json`。

- 将大脚本拆成数据读取、参数、面板、模型、批次提取、聚合、重算与独立校验模块。
- 统一为 `python -m yeast.activator`，显式传入工作簿、参数 TSV 和输出路径。
- 用标准库 OOXML 缓存值读取替代专用 Node `@oai/artifact-tool` 导出依赖。
- 去除导入时创建输出目录、固定日期的旧审计目录，以及旧包必须存在的前提。
- 将历史 notebook 公共实现移至 `yeast/analysis.py`，保留根目录兼容导入；相同质量守恒计算收敛到 `yeast/binding.py`。
- 原有批次规则、星号排除、绿色敏感性分析、单位换算、模型及主/敏感性指标分别保留。

## 修复与加强

- R² 输入长度与维度严格匹配，防止 `zip` 静默截断或 NumPy 广播；拒绝未知响应尺度和不合法布尔值。
- 校验采用显式异常，在 `python -O` 下仍然有效。没有星号测量的数据也能校验。
- 数值源单元格、参数表重复项和模型输入有明确校验；无缓存 Excel 公式报错而非猜值。
- 源快照与工作簿缓存值交叉核对；重新计算保存预测，而非只比较已保存的 R²。
- 清单使用跨平台相对路径，并记录源文件、生成文件和代码哈希；结果更新时使旧 PASS 状态失效。
- CarR 的源块限定到原始布局最后一行 296，避免把工作表后方新说明或格式行误读成剂量数据。

## 原版对照

构造明确标注为合成数据的 27 面板工作簿，覆盖普通 DBD、CI94、LBD、GR、BjaR mutant 和 CarR 的源布局，并使用合成补充表参数。将压缩包原版 `01_recalculate.py` 与整理后的工作流分别运行：

- 对照 7 张数值 CSV，逐字段比较 **407,058** 个值。
- 数值比较容差为相对/绝对 `1e-12`，缺失数值按相同缺失处理。
- **0 个数值或结构差异**；仅 2 个 KA 来源说明由历史用户指令措辞改为“原压缩包记录的默认值”。
- 合成参考输出为 9,024 条测量、2,148 条全部图型条件均值、1,824 条剂量响应条件均值和 27 个面板。测试参考摘要保存在 `tests/fixtures/activator_reference_summary.json`，不是实验结果。

## 回归测试

命令：`python -m unittest discover -s tests -v`。

本地共 **40 项测试通过**，其中本次 activator 增加 22 项；原来的 18 项 notebook/mammalian 测试继续通过。覆盖 27 面板对照、星号/绿色规则、同输入不同批次不合并、30% 绘图分组、公式缓存、源快照冲突、参数覆盖、文件篡改、独立模型预测、`python -O`、历史对比可选、无星号数据和无输出副作用的预检查。

本地 Python 3.9.15/NumPy 1.23.5 也通过；仓库推荐 Python 3.10+，现有 CI 配置为 Python 3.11。GitHub CI 结果与本地验证分开记录。

## 限制

本次附件只包含脚本，没有真实 `Source data.xlsx`、参数 TSV 或原始审计数据。因此已验证代码整理前后的数值等价性和合成输入上的完整流程，尚未验证真实实验的 27 面板结果。其他历史 notebooks 的实验参数和模型变体保留，不将其自动解释为这一 activator 工作流。
